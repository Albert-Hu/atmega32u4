# Build a Production Firmware for ATMega32u4

This project based on [LUFA](https://github.com/abcminiuser/lufa) to build a production firmware.

- Build the App:

```makefile
make app
```

- Build the bootloader:

```makefile
make bootloader
```

- Build the production firmware:

```makefile
make release
```

